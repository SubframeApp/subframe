---
title: Vite
description: "Get up and running with Vite and Subframe"
---

If you just want to get set up with Vite and Subframe quickly, you can follow the [quickstart guide](/getting-started/quickstart). If you want to set up Subframe in your Vite app manually, you can follow the steps below.

<Note>
    This guide assumes that you already have React and Tailwind set up in your Vite app. If you don't, please follow the [Vite guide for scaffolding your project](https://vite.dev/guide/#scaffolding-your-first-vite-project) and the [TailwindCSS guide for setting up Tailwind in your Vite app using PostCSS](https://v3.tailwindcss.com/docs/installation/using-postcss).
</Note>


To be able to sync our components into our Vite app, we will need to initialize the project.

The easiest way of doing so is the Subframe CLI, which will initialize the project for you and set up some necessary configurations.


<Steps>
    <Step title="Run the Subframe CLI">
        The following command will set up Subframe in your project.

        <CodeGroup>
            ```bash npm
            npx @subframe/cli@latest init
            ```
            ```bash yarn
            yarn @subframe/cli@latest init
            ```
            ```bash pnpm
            pnpm exec @subframe/cli@latest init
            ```
            ```bash bun
            bunx --bun @subframe/cli@latest init
            ```
        </CodeGroup>
        
        <Info>
            Should you run into any trouble with the automated installation, have a look at the [manual installation guide](/getting-started/framework-guides/manual).
        </Info>
    </Step>

    <Step title="Configure Vite to use import aliases">
        For Vite to understand the `@/[DIRECTORY]` import syntax, we need to configure the `compilerOptions` in our `tsconfig.app.json` file and let Vite know how to resolve the aliases in our `vite.config.ts` file.

       <CodeGroup>
         ```json tsconfig.app.json {3-8}
        {
            "compilerOptions": {
                "baseUrl": "./",
                "paths": {
                    "@/*": [
                        "src/*"
                    ]
                },
                "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
                "target": "ES2020",
                "useDefineForClassFields": true,
                "lib": ["ES2020", "DOM", "DOM.Iterable"],
                "module": "ESNext",
                "skipLibCheck": true,

                /* Bundler mode */
                "moduleResolution": "bundler",
                "allowImportingTsExtensions": true,
                "isolatedModules": true,
                "moduleDetection": "force",
                "noEmit": true,
                "jsx": "react-jsx",

                /* Linting */
                "strict": true,
                "noUnusedLocals": true,
                "noUnusedParameters": true,
                "noFallthroughCasesInSwitch": true,
                "noUncheckedSideEffectImports": true+
            },
            "include": ["src"]
        }
        ```

        ```tsx vite.config.ts {6-10}
        import { defineConfig } from 'vite'
        import react from '@vitejs/plugin-react'

        export default defineConfig({
            plugins: [react()],
            resolve: {
                alias: {
                    "@/*": "./src/*"
                }
            }
        })
        ```
       </CodeGroup>
    </Step>
</Steps>


You now have your Subframe components set up to use in your app. You can now go ahead and use them in your project.

<Card title="Using components" href="/developing-with-subframe/basics/using-components">
    Learn how to use your Subframe components in your app
</Card>