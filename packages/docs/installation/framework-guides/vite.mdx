---
title: Vite
description: Install Subframe in your Vite + React app
---

<Note>
This guide assumes you have a Vite project with React and Tailwind CSS. If not, follow [Vite](https://vite.dev/guide/#scaffolding-your-first-vite-project) and [Tailwind CSS](https://v3.tailwindcss.com/docs/installation/using-postcss) setup first.
</Note>

## Installation

Run the init command in your repository root:

<CodeGroup>
  ```bash npm
  npx @subframe/cli@latest init
  ```
  ```bash yarn
  yarn dlx @subframe/cli@latest init
  ```
  ```bash pnpm
  pnpx @subframe/cli@latest init
  ```
  ```bash bun
  bunx --bun @subframe/cli@latest init
  ```
</CodeGroup>

## Configure import aliases

After installation, configure Vite to resolve import aliases.

### Update tsconfig.app.json

Configure `compilerOptions` so TypeScript understands import aliases:

```json tsconfig.app.json {3-8}
{
  "compilerOptions": {
    "baseUrl": "./",
    "paths": {
      "@/*": [
        "./src/*"
      ]
    },
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  "include": ["src"]
}
```

### Update vite.config.ts

Install Node types and update Vite config to resolve paths:

```bash
npm install -D @types/node
```

Then update `vite.config.ts`:

```tsx vite.config.ts {7-11}
import { defineConfig } from "vite"
import react from "@vitejs/plugin-react"
import { resolve } from "node:path"

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      "@": resolve(__dirname, "./src"),
    }
  }
})
```

## Use components

Import and use Subframe components:

```tsx src/App.tsx
import { Button } from "@/ui/Button";

export default function App() {
  return <Button>Click me</Button>;
}
```

For troubleshooting, see the [Manual installation guide](/installation/framework-guides/manual).
